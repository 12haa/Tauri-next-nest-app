{
  "name": "monorepo-app",
  "private": true,
  "scripts": {
    "dev:web": "cd web && pnpm run dev",
    "dev:tauri": "cd tauri-app && pnpm run tauri dev",
    "dev": "concurrently \"pnpm run dev:web\" \"pnpm run dev:tauri\"",
    "build:web": "cd web && pnpm run build",
    "build:tauri": "cd tauri-app && pnpm run tauri build",
    "build": "pnpm run build:web && pnpm run build:tauri"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
